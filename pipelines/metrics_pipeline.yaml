description: Full metrics pipeline
inherits:
  - location: $METRIC_PIPE_DIR/pipelines/gen_inputs.yaml
  - location: $METRIC_PIPE_DIR/pipelines/matched_analysis.yaml
  - location: $METRIC_PIPE_DIR/pipelines/matched_agg.yaml
