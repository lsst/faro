description: Compute metrics from multiband matched catalogs
tasks:
  AB1:
    class: lsst.faro.measurement.PatchMatchedMultiBandMeasTask
    config:
      connections.package: validate_drp
      connections.metric: AB1
      python: |
        from lsst.faro.measurement.MatchedCatalogMetricMeasureTasks import AB1Task
        config.measure.retarget(AB1Task)
