description: Full matched metrics pipeline
inherits:
  - location: $METRIC_PIPE_DIR/pipelines/gen_inputs_matched.yaml
  - location: $METRIC_PIPE_DIR/pipelines/analysis_matched.yaml
  - location: $METRIC_PIPE_DIR/pipelines/agg_matched.yaml
